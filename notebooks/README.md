# notebooks

Scratch space for experiments. Nothing here is imported by the package.

Keep notebooks out of version control unless they contain something worth
keeping. Cleared outputs make diffs readable:

```
jupyter nbconvert --clear-output --inplace mynotebook.ipynb
```
